-Transformei o método valor() em um método da superclasse Imovel, pois tinha implementação comum a todas as subclasses.

-Criei uma variavel "const int PCT" para representar a porcentagem de comissao em cada subclasse de Imovel, pois antes era apenas um "número mágico" em algumas partes do código.

-Criei um construtor para Cliente e tornei privados os seus atributos.

-Criei construtores para as subclasses de Imovel e tornei protegidos os atributos da superclasse.

-Criei uma classe Relatorio, responsável por criar e imprimir o relatório final (essa responsabilidade antes estava na main).

-Como consequência da abstração e atribuição da responsabilidade às classes devidas, limpei toda implementação que já não era necessária na main.

-Renomeei alguns atributos de Cliente e Imovel, que ou não eram muito significativos ou não estavam padronizados com letra minúscula.

-Separei a implementação dos métodos de suas declarações. Para isso, criei novos arquivos .cpp para Imovel e suas subclasses.

-